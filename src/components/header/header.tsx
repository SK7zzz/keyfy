import { HeaderContainer, HeaderTitle } from "./header.styles";
const Header = () => {
  return (
    <HeaderContainer>
      <HeaderTitle>What do you need?</HeaderTitle>
    </HeaderContainer>
  );
};

export default Header;
